# Frontend (Vue 3 + Vite) placeholder

This folder is the intended frontend for the PWA.

To initialize locally:
1. cd frontend
2. npm install
3. npm run dev

The real Vue app will be committed in subsequent Phase 0 updates as components and build scripts.
