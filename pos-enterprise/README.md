# POS Enterprise — Offline-first skeleton (XAMPP)

This folder contains the POS Enterprise offline-first skeleton with product image management (drag-and-drop upload, client-side compression, thumbnail generation, duplicate detection, soft-delete, and server-side storage). Do not commit secrets — only .env.example is provided.

Follow the README in pos-enterprise/README.md for installation steps.
